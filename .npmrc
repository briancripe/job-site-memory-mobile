# ponytail: hoisted because some React Native libraries (e.g. react-native-markdown-renderer from
# the kit) fail to resolve under pnpm's default isolated layout, which Expo only supports from SDK 54.
# Try deleting this line once the app runs, if strict isolation matters.
node-linker=hoisted
